<section class="admin-ticket_status_codes-new">

  <header class="page-header">
    <h1>Admin &raquo; New Status Code</h1>
  </header>

  <div class="well">
    <%= link_to 'All Ticket Status Codes', [:admin, :ticket_status_codes], class: 'btn btn-info' %>
  </div>

  <div class="row">
    <div class="col-sm-6 col-sm-offset-1">
      <%= form_for [:admin, @ticket_status_codes] do |form| %>
    
        <% if form.object.errors.any? %>
          <div class="alert alert-danger">
            <strong>The following errors prevented saving:</strong>
            <ul>
              <% form.object.errors.full_messages.each do |error| %>
                <li><%= error %></li>
              <% end %>
            </ul>
          </div>
        <% end %>

        <div class="form-group">
          <%= form.label :code %>
          <%= form.text_field :code, class: 'form-control' %>
          <%= form.label :description %>
          <%= form.text_field :description, class: 'form-control' %>
        </div>

        <%= form.submit 'Create Ticket Status', class: 'btn btn-primary' %>
      <% end %>
    </div>
  </div>

</section>
