<section class="admin-ticket_types-new">

  <header class="page-header">
    <h1>Admin &raquo; New Ticket Type Code</h1>
  </header>

  <div class="well">
    <%= link_to 'All Ticket Type', [:admin, :ticket_types], class: 'btn btn-info' %>
  </div>

  <div class="row">
    <div class="col-sm-6 col-sm-offset-1">
      <%= form_for [:admin, @ticket_types] do |form| %>
    
        <% if form.object.errors.any? %>
          <div class="alert alert-danger">
            <strong>The following errors prevented saving:</strong>
            <ul>
              <% form.object.errors.full_messages.each do |error| %>
                <li><%= error %></li>
              <% end %>
            </ul>
          </div>
        <% end %>

        <div class="form-group">
          <%= form.label :code %>
          <%= form.text_field :code, class: 'form-control' %>
          <%= form.label :description %>
          <%= form.text_field :description, class: 'form-control' %>
        </div>

        <%= form.submit 'Create Ticket Type', class: 'btn btn-primary' %>
      <% end %>
    </div>
  </div>

</section>
