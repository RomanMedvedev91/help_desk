<section class="admin-qa_categories-new">

  <header class="page-header">
    <h1>Admin &raquo; New QA Category</h1>
  </header>

  <div class="well">
    <%= link_to 'All QA Categories', [:admin, :qa_categories], class: 'btn btn-info' %>
  </div>

  <div class="row">
    <div class="col-sm-6 col-sm-offset-1">
      <%= form_for [:admin, @qa_category] do |form| %>
    
        <% if form.object.errors.any? %>
          <div class="alert alert-danger">
            <strong>The following errors prevented saving:</strong>
            <ul>
              <% form.object.errors.full_messages.each do |error| %>
                <li><%= error %></li>
              <% end %>
            </ul>
          </div>
        <% end %>

        <div class="form-group">
          <%= form.label :code %>
          <%= form.text_field :code, class: 'form-control' %>

          <div class="new_ticket-text-area">
          <%= form.label :description %>
          <%= form.text_area :description, :class => "tinymce", :rows => 15, :cols => 120 %>
          <%= tinymce %>
          </div>
        </div>

        <%= form.submit 'Create QA Category', class: 'btn btn-primary' %>
      <% end %>
    </div>
  </div>

</section>
