<section class="ticket-new">

  <header class="page-header">
    <h1>Edit Product</h1>
  </header>

  <div class="well">
    <%= link_to 'All Products', [:admin, :products], class: 'btn btn-info' %>
  </div>

  <div class="row">
    <div class="col-sm-6 col-sm-offset-1">
      <%= form_for [:admin, @product] do |form| %>
    
        <% if form.object.errors.any? %>
         <div class="alert alert-danger">
            <strong>The following errors prevented saving:</strong>
            <ul>
              <% form.object.errors.full_messages.each do |error| %>
                <li><%= error %></li>
              <% end %>
            </ul>
          </div>
        <% end %>

        <div class="form-group">

          <div class="new_ticket-selector col-2">
          <%= form.label :category_id %>
          <% options = options_from_collection_for_select(@categories, 'id', 'code', @product.category_id) %>
          <%= form.select :category_id,  options, {}, { class: 'form-control' } %>
          <div></div>
          </div>

          <div class="form-selector">
          <%= form.label :code %>
          <%= form.text_field :code, class: 'form-control' %>
          </div>
         
          <%= form.label :model %>
          <%= form.text_field :model, class: 'form-control' %>

          <div class="new_ticket-text-area">
            <%= form.label :description %>
            <%= form.text_area :description, :class => "tinymce", :rows => 15, :cols => 120 %>
            <%= tinymce %>
            </div>


        </div>

        <%= form.submit 'Update Product', class: 'btn btn-primary' %>
      <% end %>
    </div>
  </div>

</section>
